// Define global variables for use throughout gameplay

global.gameState = MENU;  // For use in game state system
if(instance_exists(objBtnStart))
{
    global.buttonStart = objBtnStart;   // Set Start button as a global variable
}
